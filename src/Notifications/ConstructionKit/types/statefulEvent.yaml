"$schema": "https://schemas.meshmakers.cloud/construction-kit-elements.schema.json"
types:
  - typeId: StatefulEvent
    derivedFromCkTypeId: ${this}/Event
    isFinal: false
    isAbstract: false
    attributes:
      - id: ${this}/EventState
        name: State

